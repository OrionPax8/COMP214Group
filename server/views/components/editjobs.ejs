  <!-- Main Content -->
  <main class="container">
    <div class="row">
        <div class="offset-md-1 col-md-11">
            <h1><%= title %></h1>
            <br>
            <table class="table table-bordered table-striped table-hover">
                <!-- Header Row -->
                <table class="table">
                <thead>
                    <tr>
                    <th>Job ID</th>
                    <th>Job Title</th>
                    <th>Minimum Salary</th>
                    <th>Maximum Salary</th>
                    <th></th>
                    <th></th>
                    </tr>
                </thead>
            
            <!--<tbody>
            <%// for(let count = 0; count < employeeList.length; count++){%>
            <tr> 
                <td> <input type="text" class="form control" id="jobID<%// count %>" value="<%//= employeeList[count].EMPLOYEE_ID %>"></td>
                <td> <input type="text" class="form control" id="jobTitle<%// count %>" value="<%//= employeeList[count].FIRST_NAME %></td>
                <td> <input type="text" class="form control" id="minSal<%// count %>" value="<%//= employeeList[count].LAST_NAME %></td>
                <td> <input type="text" class="form control" id="maxSal<%// count %>" value="<%//= employeeList[count].EMAIL %></td>
                <td> <a href="#" class="btn btn-primary">Update</a></td>
                <td><form action="#" method="POST"><button type="submit" class="btn btn-danger">Delete</button></form></td>
            </tr>
            <% //} %>
            </tbody>
            -->
            </table>
        </div>
    </div>
  </main>
