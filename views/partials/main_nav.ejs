  
  <!-- Main Navigation -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto mb-2 mb-lg-0">

          
          <% if (title == "Home") { %>
            <li class="nav-item active">
          <% } else { %>
            <li class="nav-item">
          <% } %>          
            <a class="nav-link" aria-current="page" href="/home"><i class="fa-solid fa-lg fa-house"></i> Home</a>
          </li>
          
          <% if (title == "Hire Employee") { %>
            <li class="nav-item active">
          <% } else { %>
            <li class="nav-item">
          <% } %>          
            <a class="nav-link" aria-current="page" href="/hire"><i class="fa-solid fa-lg"></i> Hire Employee</a>
          </li>

          <% if (title == "Employee List") { %>
            <li class="nav-item active">
          <% } else { %>
            <li class="nav-item">
          <% } %>          
            <a class="nav-link" aria-current="page" href="/emp-list"><i class="fa-solid fa-lg"></i> Current Employees</a>
          </li>

          <% if (title == "Services") { %>
            <li class="nav-item active">
          <% } else { %>
            <li class="nav-item">
          <% } %>          
            <a class="nav-link" aria-current="page" href="/services"><i class="fa-solid fa-lg fa-gear"></i> Servies</a>
          </li>

          <% if (title == "Contact Me") { %>
            <li class="nav-item active">
          <% } else { %>
            <li class="nav-item">
          <% } %>          
            <a class="nav-link" aria-current="page" href="/contact"><i class="fa-solid fa-lg fa-phone"></i> Contact Me</a>

            <% if (typeof displayName == 'string' && displayName) { %>

              <% if (title == "Business Contacts") { %>
                <li class="nav-item active">
              <% } else { %>
                <li class="nav-item">
              <% } %>          
                <a class="nav-link" aria-current="page" href="/contact-list"><i class="fa-solid fa-lg fa-briefcase"></i> Busniess Contacts</a>

              <li class="navbar-test navbar-dark" style="color: white;"><i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %></li>
              <li class="nav-item"><a href="/logout" class="nav-link"><i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li>
              <% } else  { %>
                <li class="nav-item"><a href="/login" class="nav-link"><i class="fas fa-lg fa-sign-in-alt"></i> Login</a></li>
              <% } %>
          </li>
      </div>
    </div>
  </nav>
